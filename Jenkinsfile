pipeline {
    agent any

    stages {
        stage('Manual-1') {
            steps {
                echo "MANUAL TRIGGERED"
                echo "Branch: ${env.BRANCH_NAME}"
            }
        }
    }
}
